export enum Colors {
    BaseColor = 'Base Color',
    HighlightColor = 'Highlight Color',
}

export enum ColorFields {
    red = 'r',
    green = 'g',
    blue = 'b',
}

export enum Shape {
    Circle = 'Circle',
    Square = 'Square',
}
